# Builds the module for llvm.
!include clang.yaml
!include rcic-module.yaml
---
- package: Clang module
  category: COMPILERS
  release: 3
  module:
    prereq: 
      - *ModuleCompiler_prereq
    requires: 
      - *ModuleCompiler_requires
