# Builds the module for llvm.
!include llvm.yaml
!include rcic-module.yaml
---
- package: LLVM module
  category: COMPILERS
  module:
    <<: *ModuleCompiler
    setenv:
       - LLVM_DIR {{root}}/lib/cmake/llvm

