---
versions: versions-extra.yaml
bootstrap:
   - llvm
   - llvm-module
build:
   - clang 
   - clang-module 
   - boost
   - boost-module
   - mpich
   - mpich-module
manifest:
   - "{{bootstrap}}"
   - "{{build}}"
